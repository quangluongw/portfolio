import { AboutSection } from "@/components/AboutSection";

const page = () => {
    return (
      <section className="relative overflow-hidden mt-40 mb-20">
        <AboutSection />
      </section>
    );
};

export default page;